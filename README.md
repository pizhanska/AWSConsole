Work with Maven wrapper

-use mvnv instead mvn

Allure report ----------------------------------------------------------------------------------------------------------

-at first run the tests (mvnw clean test)
-generate report (mvnw allure:report)
-generate report and start server (mvnw allure:serve)